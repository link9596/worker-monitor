{
  "name": "worker-monitor",
  "version": "1.0.0",
  "description": "Send email via QQ SMTP on Cloudflare Worker",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.js --bundle --outfile=worker.js --platform=neutral --external:node:* --external:cloudflare:sockets"
  },
  "dependencies": {
    "@workermailer/smtp": "^0.2.1",
    "esbuild": "^0.19.0"
  }
}

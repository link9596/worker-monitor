name = "worker-monitor"
main = "index.js"
compatibility_date = "2024-12-18"
compatibility_flags = ["nodejs_compat"]

# KV 命名空间绑定
[[kv_namespaces]]
binding = "USAGE_ALERT_STATE"
id = "你的 KV 命名空间 ID"

# Cron 定时触发器（每 30 分钟）
[triggers]
crons = ["*/30 * * * *"]
